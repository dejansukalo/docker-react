sudo: required

services:
  - docker

before_install:
  - docker build -t dejansukalo/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true dejansukalo/docker-react npm run test -- --coverage 